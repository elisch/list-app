import colors from './colors.js';

const commonStyles = {
  box: {
    width: '100%',
    marginTop: 13,
    padding: 25,
    backgroundColor: colors.white,
    borderRadius: 32,
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 5,
    },
    shadowOpacity: 0.25,
    shadowRadius: 13,

    elevation: 5,
  },
};

export default commonStyles;
